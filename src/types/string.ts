const album: string = "Atom Heart Mother";
const favAlbum: string = "Animals";
const band = "Pink Floyd";

const details: string = `I'm currently listenting to the album ${album}, but my favourite album from ${band} is ${favAlbum}`;

console.log(details);
